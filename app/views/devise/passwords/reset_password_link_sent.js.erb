$('#new_user_session').modal('hide');
<%= render 'shared/modal', modal_id: 'large', body:
  escape_javascript(render 'devise/passwords/reset_password_link_sent'), title: _("Password reset instructions") %>
